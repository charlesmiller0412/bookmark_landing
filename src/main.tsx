import React from "react";
import ReactDOM from "react-dom/client";
import BookmarkLanding from "./BookmarkLanding";
import "./styles/output.css";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
    <React.StrictMode>
        <BookmarkLanding />
    </React.StrictMode>
);
